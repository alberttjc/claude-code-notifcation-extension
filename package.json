{
  "name": "claude-permission-popup",
  "displayName": "Claude Code Permission Popup",
  "description": "Shows VS Code modal dialogs for Claude Code permission requests",
  "version": "0.0.2",
  "publisher": "claude-code-contrib",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/claude-code-contrib/claude-permission-popup"
  },
  "bugs": {
    "url": "https://github.com/claude-code-contrib/claude-permission-popup/issues"
  },
  "homepage": "https://github.com/claude-code-contrib/claude-permission-popup#readme",
  "categories": [
    "Other"
  ],
  "keywords": [
    "claude",
    "claude-code",
    "permission",
    "hooks",
    "modal"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "extensionKind": ["workspace"],
  "main": "./extension.js",
  "scripts": {
    "package": "vsce package"
  },
  "contributes": {
    "configuration": {
      "title": "Claude Permission Popup",
      "properties": {
        "claudePermissionPopup.port": {
          "type": "number",
          "default": 0,
          "description": "Port for the permission request HTTP server. Set to 0 (default) for a random available port. The actual port is written to a runtime file for the hook script to discover automatically."
        },
        "claudePermissionPopup.modalTimeout": {
          "type": "number",
          "default": 300000,
          "description": "Timeout in milliseconds for the permission modal dialog. If no response within this time, the modal is dismissed and falls back to terminal prompt. Default: 300000 (5 minutes)."
        }
      }
    }
  }
}
